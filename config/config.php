<?php

define('DEBUG',true);

define('DEFAULT_CONTROLLER','Home');//default controller if not defined

define('DEFAULT_LAYOUT','default'); // if no layout is set in the controller use this $_layout

define('SROOT','/'); //set this to '/' for a live server

define('SITE_TITLE','Skeleton MVC Framework'); // if no site title is set
